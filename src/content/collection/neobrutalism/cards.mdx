---
title: Cards
description: Neobrutalism-style card components built with Tailwind CSS. Featuring thick borders, hard shadows, and distinct layouts. Great for displaying content, products, and features with a unique, high-contrast retro style.
category: neobrutalism
slug: cards
terms:
  - card
  - container
components:
  - { title: 'Base', dark: true }
  - { title: 'Base stacked', dark: true }
  - { title: 'Base dashed', dark: true }
  - { title: 'Retro window', dark: true }
---

import BaseHero from '../../../components/BaseHero.astro'
import BackLink from '../../../components/BackLink.astro'

<BaseHero title={frontmatter.title} description={frontmatter.description}>
  <BackLink
    slot="back"
    href={`/components/${frontmatter.category}`}
    label={`Back to ${frontmatter.category} components`}
  />
</BaseHero>
